#include "main.h"
#include <string.h>
/**
 * _strcat- concatenates src to dest
 * @src: files to be appended
 * @dest: file to hold appended char
 * Return: 0 always
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
