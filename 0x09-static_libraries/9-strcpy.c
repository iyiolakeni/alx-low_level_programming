#include <string.h>
#include "main.h"
/**
* _strcpy- prints half of a string
* @dest: destination
*
* @src: source
* Return: dest
*/
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
