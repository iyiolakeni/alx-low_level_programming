#include <string.h>
#include "main.h"

/**
 * _strchr - function to copy memory area
 * @s: destination
 * @c: source
 * Return: c pointer or null
*/
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
